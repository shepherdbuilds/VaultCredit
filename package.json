{
  "name": "vaultcredit-hardhat",
  "version": "1.0.0",
  "description": "VaultCredit — Privacy-preserving credit scoring using Zama fhEVM",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "node": "hardhat node",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.8"
  },
  "dependencies": {
    "@fhevm/solidity": "^0.11.1",
    "fhevm": "^0.5.8"
  }
}
